import { Box } from "@mui/material";

export const ProgressBar = () => {
  return (
    <Box
      sx={{
        // width: "100%",
        marginInline: "10px",
        backgroundColor: "rgba(255, 255, 255, 0.45)",
        height: "10px",
      }}></Box>
  );
};
